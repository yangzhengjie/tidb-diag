# 项目协作规则

## 工程原则

以下规则适用于本仓库中的编码、调试、代码评审和重构任务。

### 先思考

- 实现前说明会影响方案的关键假设。
- 仅当歧义会实质性改变结果或操作存在风险时提问；否则采用保守假设并明确说明。
- 存在满足需求的更简单方案时，说明理由并优先采用。

### 简单优先

- 使用完整满足需求的最小实现。
- 不增加需求之外的功能、抽象、灵活性或配置项。
- 不为纯属臆测的场景增加处理逻辑；保留项目已有的校验和错误处理惯例。

### 最小改动

- 只修改任务直接需要的内容，并保留用户已有的无关改动。
- 遵循仓库现有的风格、约定和局部指令。
- 不重构、清理或格式化无关代码；发现无关问题时只作说明。
- 只移除由当前改动造成的未使用代码。

### 基于验证完成

- 对非简单任务先定义具体、可验证的成功标准。
- 修复缺陷时，在条件允许的情况下先复现问题，再验证修复。
- 在能提供有效覆盖时新增或更新聚焦的测试。
- 声明完成前运行相关检查并查看输出。
- 无法执行验证时，明确说明未验证的内容及原因。

## Git 提交信息

在本项目中创建或建议 Git 提交信息时，默认使用简体中文。

### 格式

可以采用 Conventional Commits 类型前缀，描述与正文使用中文：

```text
<type>[可选 scope]: <中文简述>

[可选正文，中文]

[可选 footer]
```

示例：

- `docs: 整理诊断智能体需求文档`
- `feat(handler): 实现只读 SQL HTTP 接口`
- `fix(policy): 修复多语句检测误判`

### 要求

- 标题使用简体中文和祈使语气，保持简明，建议不超过 72 个字符。
- 正文和 footer 需要补充说明时，同样使用中文。
- 类型前缀可以保留英文，如 `feat`、`fix`、`docs`。
- 用户明确要求使用其他语言时，以用户要求为准。
- 不得在提交信息中写入密钥、`.env`、真实 DSN、密码或其他敏感信息。

### 禁止 AI 署名

- 禁止在提交信息或 footer 中添加由 Cursor、Codex、GitHub Copilot 或其他 AI 工具生成的 `Co-authored-by` 署名。
- 执行 `git commit` 时不得自动附加此类署名；如果模板或工具自动注入，必须在提交前移除。
- 禁止的示例包括但不限于：
  - `Co-authored-by: Cursor <cursoragent@cursor.com>`
  - `Co-authored-by: Cursor cursoragent@cursor.com`
